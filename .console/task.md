# Task

## Objective

Phase 1: Build the core DAGExecutor package — loader, graph, node runners, executor, evidence.

## Definition of Done

- All source modules under `src/dag_executor/` are complete
- Test suite passes (loader, graph, variables, nodes, executor, evidence)
- `pyproject.toml` declares all dependencies
- Git initialized, committed on `main`
